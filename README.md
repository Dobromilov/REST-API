# Первый REST API

---

## Словарь изученных терминов

##  Docker

**Docker** — платформа для создания, развертывания и управления приложениями в изолированных средах, называемых "контейнерами". Позволяет запустить наше приложение с базой данных одинаково на любой машине.

**Docker Compose** — инструмент для определения и запуска многоконтейнерных приложений Docker. Мы используем файл `docker-compose.yml`, чтобы одной командой поднять и базу данных, и наше приложение.

**Контейнер** — легковесная, изолированная среда, в которой работает приложение (например, наша PostgreSQL база данных).

## 🗄️ База данных (Database)

**PostgreSQL (Postgres)** — реляционная система управления базами данных (СУБД), которую мы используем в этом проекте. Надежная, с открытым исходным кодом.

**init.sql** — SQL-скрипт, который автоматически выполняется при первом запуске контейнера с базой данных. В нем мы создаем таблицы, индексы, добавляем тестовые данные.

**Миграция (Migration)** — практика управления изменениями схемы базы данных (создание/изменение таблиц) с помощью версионированных скриптов. Пока мы делаем это через `init.sql`, но часто используются инструменты вроде `golang-migrate`.

**Схема (Schema)** — структура базы данных: какие таблицы есть, какие в них столбцы, типы данных и связи между таблицами.

##  Backend на Go

**Модель (Model)** — структура (`struct`) в Go, которая отражает таблицу в базе данных. Она описывает поля (столбцы) и их типы. Например:
```go
type Task struct {
    ID          int       `json:"id" db:"id"`
    Title       string    `json:"title" db:"title"`
    Description string    `json:"description" db:"description"`
    Completed   bool      `json:"completed" db:"completed"`
    CreatedAt   time.Time `json:"created_at" db:"created_at"`
    UpdatedAt   time.Time `json:"updated_at" db:"updated_at"`
}
```

**Handler (Обработчик)** — функция в Go, которая принимает HTTP-запрос (`http.Request`), обрабатывает его (например, обращается к базе данных) и возвращает HTTP-ответ (`http.ResponseWriter`). Это "мозг" нашего API.

**Database Driver (Драйвер БД)** — библиотека, которая позволяет Go-приложению общаться с конкретной базой данных.

##  REST API (Слой взаимодействия)

**API (Application Programming Interface)** — набор правил и инструментов, которые позволяют одним приложениям взаимодействовать с другими. Наш сервер предоставляет API для клиентов (браузера, мобильного приложения).

**REST API (RESTful API)** — архитектурный стиль для проектирования сетевых интерфейсов. Он основан на стандартах HTTP (GET, POST и т.д.) и концепции "ресурсов" (например, `/users`).

**Эндпоинт (Endpoint)** — конкретный URL-адрес, по которому API предоставляет доступ к определенной функции. Например, `GET /api/v1/users` — эндпоинт для получения списка пользователей.

**HTTP-метод (HTTP Method)** — глагол, который определяет тип операции над ресурсом:
- **GET** — получение данных.
- **POST** — создание новой записи.
- **PUT / PATCH** — обновление существующей записи (полное/частичное).
- **DELETE** — удаление записи.

**Маршрутизация (Routing)** — процесс определения того, какой обработчик (handler) должен выполниться для конкретного URL-адреса и HTTP-метода. В Go для этого часто используют роутер, например, `gorilla/mux` или `chi`.

**Запрос (Request)** — то, что клиент отправляет на сервер. Содержит:
- URL (эндпоинт)
- Метод (GET, POST...)
- Заголовки (Headers, например, `Content-Type: application/json`)
- Тело (Body) — данные, например, JSON для создания пользователя.

**Ответ (Response)** — то, что сервер отправляет обратно клиенту. Содержит:
- Код статуса (Status Code, например, `200 OK`, `404 Not Found`, `500 Internal Server Error`)
- Заголовки
- Тело — часто JSON с запрошенными данными или сообщением об ошибке

**JSON (JavaScript Object Notation)** — легкий формат для обмена данными, основанный на синтаксисе JavaScript. Стандарт де-факто для данных в REST API. Пример:
```json
{
    "id": 1,
    "name": "Ivan"
}
```

**CRUD** — акроним для четырех базовых операций с данными: **C**reate (Создать), **R**ead (Прочитать), **U**pdate (Обновить), **D**elete (Удалить). Любое простое REST API реализует эти операции для своих ресурсов.